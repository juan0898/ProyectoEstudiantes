import { Component } from '@angular/core';

// Componente de Angular para mostrar los responsables
@Component({
  selector: 'app-responsables',// Etiqueta HTML personalizada para el componente
  standalone: true,// Componente autónomo, no necesita módulos externos
  imports: [],
  templateUrl: './responsables.component.html',// Ubicación del archivo HTML
  styleUrl: './responsables.component.css'// Ubicación del archivo CSS
})
export class ResponsablesComponent {

}
